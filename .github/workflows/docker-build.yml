name: Build and Deploy to Minikube

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}
          registry: ghcr.io

      - name: Build and push Docker image to GHCR
        uses: docker/build-push-action@v5
        with:
          context: sinatra-app
          dockerfile: Dockerfile
          push: true
          tags: ghcr.io/triplemcoder14/qacetech-sinatraapp:latest

      - name: Set up Minikube certificates
        run: |
          mkdir -p $HOME/.minikube/profiles/minikube
          echo "$MINIKUBE_CLIENT_CRT" > $HOME/.minikube/profiles/minikube/client.crt
          echo "$MINIKUBE_CLIENT_KEY" > $HOME/.minikube/profiles/minikube/client.key
          echo "$MINIKUBE_CA_CRT" > $HOME/.minikube/ca.crt
        env:
          MINIKUBE_CLIENT_CRT: ${{ secrets.MINIKUBE_CLIENT_CRT }}
          MINIKUBE_CLIENT_KEY: ${{ secrets.MINIKUBE_CLIENT_KEY }}
          MINIKUBE_CA_CRT: ${{ secrets.MINIKUBE_CA_CRT }}

      - name: Get Kubeconfig from Minikube
        run: |
          mkdir -p $HOME/.kube
          minikube update-context
        env:
          KUBEVERSION: "v1.23.0"

      - name: Verify connection to Minikube cluster
        run: |
          kubectl cluster-info
          kubectl get nodes

      - name: Deploy to Minikube
        run: |
          kubectl apply -f k8s/sinatra-deployment.yml
          kubectl apply -f k8s/service-account.yml
          kubectl apply -f k8s/role.yml
          kubectl apply -f k8s/role-binding.yml

      - name: Test service URLs
        run: |
          # List services to verify the namespace and service name
          minikube service list -n default
          # Test the specific service URL
          minikube service local-devex-svc -n default --url
          echo "------------------opening the service------------------"
          curl $(minikube service local-devex-svc -n default --url)
